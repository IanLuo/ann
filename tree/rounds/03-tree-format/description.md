# TREE FORMAT — the engine's data contract

- id: `03-tree-format` · status: **done** (R3 — round gate met: tree-format-spec v1 locked @ ba528d1; superseded by v2)
- round: R3 · input: R2's artifact (requirements-spec, esp. format stability)
- intent: freeze the tree format as a locked spec — first the bootstrap contract, then superseded by the rounds model (v2)
- artifacts: `artifacts/tree-format-spec.md` (v1, locked @ ba528d1, **superseded**) · v2 lives at `04-system-design/00/01-format-amendment-v2/artifacts/tree-format-spec-v2.md`
- tasks: `01-depth-policy/` (draft 11c3de2 — depth budget, sibling-correction, pruning; absorbed into v2)
- rounds chain: ← `02-grilling` · → `04-system-design` (R4)
- search terms: tree format, rounds, epics, node schema, events, immutable, append-only, status derivation, artifact gate, checkpoint, RPO, description card, depth policy, sibling correction, pruning
