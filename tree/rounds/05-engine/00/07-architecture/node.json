{
  "id": "05-engine/00/07-architecture",
  "contract": {
    "intent": "Produce the architecture spec — repo layout, layers, and module map: where code lives (src/ structure), how the 12 components (ann-system-design §1) map to modules, entry points, and how the S1-S9 slices map to files. The build's foundation — slice 1 cannot start without it.",
    "acceptanceCriteria": [
      "architecture.md artifact exists at artifacts/ — repo tree, layer boundaries, module ownership, entry points, slice->module mapping",
      "Every component from ann-system-design §1 has a module home",
      "The functional-spec's functions map to modules (function -> module table)",
      "Layering rules stated: dependencies point one way (store <- kernel <- engines <- adapters <- cli); no cycles",
      "Test/evals layout stated (fixture suite home, S9)",
      "Spec locked (type=architecture, upstream functional-spec + ann-system-design); indexed; resolvable"
    ],
    "targetAreas": ["artifacts/architecture.md", "AGENTS.md", "src/ (created by the build)"],
    "requiredInputs": [
      "05-engine/00/06-functional-spec/artifacts/functional-spec.md (locked — functions + views)",
      "04-system-design/00/02-system-design-doc/artifacts/ann-system-design.md (locked — components, interfaces)",
      "05-engine/00/04-format-amendment-v3/artifacts/tree-format-spec-v3.md (locked — data contract)"
    ],
    "expectedOutputs": ["architecture.md (locked, complete)"]
  },
  "createdAt": "2026-08-16"
}
