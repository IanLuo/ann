# ARCHITECTURE — repo layout & module map

- id: `05-engine/00/07-architecture` · status: **queued** (blocked on functional-spec lock)
- task of round 5 · group position: 07
- intent: the **architecture spec** — src/ structure, layers, module ownership, entry points, slice→module mapping
- origin: /grill audit — components designed (system-design) but no module map; the build can't start without it
- deliverable: `artifacts/architecture.md` (locked)
- search terms: architecture, repo, layout, layers, modules, src, entry point, slice, dependency, one-way
