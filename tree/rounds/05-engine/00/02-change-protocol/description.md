# REQUIREMENTS-CHANGE PROTOCOL — handle change without rewriting history

- id: `05-engine/00/02-change-protocol` · status: **active** (draft written; lock pending)
- task of round 5 · group position: 02 (after flow-control — the change flow is a flow)
- intent: lock how Ann handles **requirements change** (and change to any locked artifact) without violating immutability
- origin: session violation — locked docs edited in place + locks rotated; the user called it: "that violates immutable and append-only… that should be a real use case"
- deliverable: `artifacts/requirements-change-protocol.md` (locked)
- search terms: change, amendment, supersede, immutable, append-only, requirements change, locked artifact, rotation, stamp repair
