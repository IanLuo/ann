# RESOURCE-REGISTRY SPEC — the management pattern

- id: `05-engine/00/14-resource-registry-spec` · status: **active** (draft written; lock pending)
- task of round 5 · group position: 14
- intent: lock the **general registry pattern** (SST per rule/resource class · consumers reference · config manages) — applies to rules, ladders, adapters, models, flows, bindings, surfaces; rule registry = instance #1
- origin: root cause — the tech stack fell through the seam (no layer owned 'what to ask'); the fix is one registry, not four patches; and the pattern is general
- deliverable: `artifacts/resource-registry-spec.md` (locked)
- search terms: registry, resource, rule, management, single source of truth, config, adapter, ladder, provenance, high-impact-defaulted
