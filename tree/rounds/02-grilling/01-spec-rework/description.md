# SPEC REWORK — fresh requirements spec

- id: `02-grilling/01-spec-rework` · status: **active** (draft written; lock pending)
- task of round 2 · group position: 01
- intent: write the **fresh requirements spec** clean-room — elicited rung-by-rung; existing docs = reference only, not template
- supersedes: `artifacts/ann-spec.md` (accreted draft) — complete-artifact rule
- key fresh decisions: self-similarity invariant (Ann's output structure = Ann's own structure) · configurable step chain (default template: idea → validate → envision → detailed specs → continue) · multi-user = design constraint (not v1) · no node limit · big-data perf N/A v1
- deliverable: `artifacts/requirements-spec.md` (locked)
- search terms: fresh spec, requirements, self-similarity, dogfooding, configurable steps, idea, validate, envision, multi-user, K1, K2, K3, K4
