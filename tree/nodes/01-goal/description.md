# GOAL — build Ann

- id: `01-goal` · status: **done** (derived: `events.jsonl` tail)
- intent: build a **tree-of-steps planning system** — the tree is plan + project memory + observer
- success: a downstream runner completes the user's goal using only tree-derived context (context packets + artifacts + ordinary project access)
- linked: `design` §21 (canonical v2 model, locked), `AGENTS.md` (intent + invariants + dogfooding)
- children: `01-grilling/`
- search terms: goal, ann, tree-of-steps, planning system, runner, dogfooding
