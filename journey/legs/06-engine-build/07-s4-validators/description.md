# 07-s4-validators

- id: `06-engine-build/07-s4-validators` · status: queued · type: task
- summary: Validators (S4) — deterministic checks served in F7 (gates) + F16 (check): schema, artifact gate, append-only, leg gate, distance-to-goal as set, redaction, artifact-hash integrity. Consumes the rule registry (rules/check/rules.json) + vocab registry. Refuses judgment calls (that is the runner reviewer job) and scalar progress numbers (AC5).
- search terms: Validators, deterministic, checks, served, gates, check, schema, artifact
