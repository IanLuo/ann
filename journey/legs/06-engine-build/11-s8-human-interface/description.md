# 11-s8-human-interface

- id: `06-engine-build/11-s8-human-interface` · status: queued · type: task
- summary: Human interface + Renderers (S8) — presents step cards + artifacts at the human gates (GATE1 grilling, GATE2 confirm-result); collects accept/reject + feedback (the talk-loop, F7); talk v1 with web HTML via the same adapter (web UI = target surface); tree view, step cards, full plan renders (F10/F11/F12). Refuses to skip a gate, fabricate an acceptance, or invent feedback; never renders scalar progress (AC5) or secrets.
- search terms: Human, interface, Renderers, presents, step, cards, artifacts, human
