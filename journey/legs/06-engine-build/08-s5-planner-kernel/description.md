# 08-s5-planner-kernel

- id: `06-engine-build/08-s5-planner-kernel` · status: queued · type: task
- summary: Planner kernel + Flow config + Provider adapter (S5) — orchestrates node lifecycle per flow-control-spec v4 (spawn to materialize to GATE1 to validate to activate to execute to verify to GATE2 to commit to look-back); frontmost-ready selection; bounded rework + chain append; owns spec expansion (F9); consumes per-project flow config (data, not code — AC-3); uniform LLM interface with multiple providers + per-task model selection (F17).
- search terms: Planner, kernel, Flow, config, Provider, adapter, orchestrates, node
