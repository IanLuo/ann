# GOAL — build Ann

- id: `01-goal` · status: **done** (R1 — round gate met: design §21 locked @ eb07146)
- round: R1 of the project chain · this round = the first step; every later round is based on it
- intent: build a **tree-of-steps planning system** — the table of rounds (epics), each round a step forward, gated on its goal
- success: a downstream runner completes the user's goal using only tree-derived context (context packets + artifacts + ordinary project access)
- artifact: `artifacts/design.md` (model spec v3, locked @ a414a0b) + `artifacts/goal.md` (refined goal + success definition)
- rounds chain: → `02-grilling` (R2) → `03-tree-format` (R3) → `04-system-design` (R4) → …
- linked: `design`, `AGENTS.md` (intent + invariants + dogfooding)
- search terms: goal, ann, tree-of-steps, rounds, epics, planning system, runner, dogfooding
