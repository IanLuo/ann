{"at":"2026-09-10","type":"created","note":"spawned by bookkeeper (agent)"}
{"at":"2026-09-10","type":"submitted","gate":"grill","note":"submitted for the grill gate (agent)"}
{"at":"2026-09-10","type":"confirmed","gate":"grill","feedback":"grilled accept: the two-tier writing rule is the guide — commands where a write is a gate/structure transition (spawn/submit/gate/goal) or a COMMON GESTURE; shaped facts stay on append! (activated/waiting are frame-internal; extended/gate-revised/transferred/deferred are rare closure facts). Evidence is the biggest gap: every task records commits[] and we hand-write the JSON. Completed is the second: the done terminal, hand-written, and its absence is exactly the accepted-but-uncompleted trap 08/01 fixes. This task adds evidence! (validated commits[] shape, thin front over the store) and resolves completed — either complete! with named preconditions or gate! accept auto-completing — decided against 08/01's honest intermediate status and RECORDED. No new write path.","note":"accepted (agent)"}
{"at":"2026-09-10","type":"evidence","note":"The close gesture commands delivered (e87f26a). TWO commands replace the hand-written append! JSON every task used to conclude, per the surface rule the task states (the two-tier writing model): a dedicated command exists where a write is a GATE/STRUCTURE transition or a COMMON GESTURE, everything else stays a shaped fact on append! — now documented in the L1 header (src/commands/index.ts) and the README command section. (1) evidence! <id> <sha>[,<sha>] [--refs a.md,b.md] [--note '<text>'] — the CONCLUSION record (F-AC18): a thin VALIDATED FRONT, never a second validator. The shape stays the single writer's (the store tightened evidence.commits[] to NON-EMPTY with a sha per entry, so append! and the command enforce the same one rule — proven by a test that goes around the command through append! and still gets refused), and the command adds only the gesture's precondition (named refusals: no-commits / bad-commit / bad-ref / leg-gate-write / no-node) plus RECORDED_BY provenance (default note names the initiator). Fully-formatted JSON, per-commit notes, refs/answers/trace-only evidence stay on append!. (2) complete! <id> [--note '<text>'] — the DONE terminal, and AC-2 RESOLVED: gates decide, commands complete — gate! confirm accept does NOT auto-complete. complete! refuses unless the confirm gate's LAST decision is an accept (not-accepted, naming none/rejected/submitted: a re-submission after an accept cannot be completed over) AND the task carries conclusion evidence (no-evidence — the SAME F-AC18 predicate the store's spawn gate and check() read, so one predicate, three consumers), and it refuses a second close (already-completed). Consequences, deliberate: completed always cites a real gate decision and real committed work (GATE-2 and F-AC18 hold by construction); gate! stays a PURE decision (one event per gesture, no state-dependent side write whose tail depends on hidden evidence state); the frame's own confirmAndCommit path is untouched; and leg 08 task 01's honest accepted status stands as the word that NAMES the missing gesture (evidence! then complete!) instead of hiding it behind an auto-complete. Rejected alternative recorded: auto-complete on accept when evidence is present — the argument FOR it is consistency with the frame's own rule (confirm accepted + evidence present -> completed) and one fewer operator gesture; it was rejected because it collapses the human's decision into the delivery record (completed would no longer be a distinct, auditable act) and makes the accepted word nearly unreachable, i.e. it would partially obsolete task 01. AC-3: both commands appear in the derived surface table (ann commands, regenerated in README) with the two-tier rule; they add NO new write path — both compose the same L1 store write (appendEvent -> the single writer) the append! path uses, verified by the event tail + a clean check; the read-only journey guard covers them (session-read e2e); and the e2e/unit flows that hand-wrote evidence/completed JSON are migrated to the gestures where they represent the normal gesture (cli.e2e lifecycle + the new close-command e2e, uniform-json + parity lifecycle drivers, session-read read-only list, the eval harness operator-commit, the frame/steps unit fixtures; the GATE-2 refusal test stays on append! by design, and the intent-translator test stays because append! IS its subject). The two user-facing wait-guidance strings (the advance! landing card and the frame's waiting note) now name evidence! instead of raw JSON. AC-2's decision is also recorded in code where it is enforced and in the README close-flow prose. Verification (fresh): full suite 575 green / 35 files (was 565/35 — +9 command-surface tests: evidence! happy path + named refusals + the single-shape-rule proof, complete! preconditions (none/rejected/re-submitted-after-accept/no-evidence/already-completed/leg-root/unknown-node), the AC-2 no-auto-complete proof (evidence + submit + accept leaves accepted; complete! then done; check clean), and the no-new-write-path read; +1 cli.e2e driving both commands through the binary incl. usage + write-marker refusals and a clean check), npm run typecheck clean, ann check OK (13 docs in the manifest, no gate gaps), ann verify clean (0 drifts). Also fixed in passing, observed twice in this session (and once at baseline): the parity + uniform-json e2e matrices are spawn-bound (~45-50 real-binary spawns) and exceeded the 5s default under the suite's parallel files — both now carry an explicit 30s budget.","commits":[{"sha":"e87f26a"}]}
{"at":"2026-09-10","type":"submitted","gate":"confirm","note":"submitted for the confirm gate (agent)"}
