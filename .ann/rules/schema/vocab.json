{
  "registry": "vocab",
  "version": 3,
  "category": "schema",
  "kind": "registry",
  "definition": "the schema vocabulary — the single source of truth for event types, statuses, gates, and artifact types (resource-registry spec: one registry per class, no layer duplicates). Machine-readable form of journey-format-spec v14 §3. Data-or-code is PER KEY (resource-registry v3 §5): artifactTypes is ADJUSTABLE DATA — each entry carries the docs/ category it places into and whether it takes a -v<N> filename (format v14 §14/§15); eventTypes, statuses and the gate SET/POSITIONS are PROTECTED — enforced as code literals and recorded as reconciliations. Consumers read; never hardcode.",
  "eventTypes": [
    "created",
    "activated",
    "extended",
    "evidence",
    "artifact-locked",
    "completed",
    "failed",
    "waiting",
    "superseded",
    "submitted",
    "confirmed",
    "rejected",
    "gate-revised",
    "transferred",
    "deferred"
  ],
  "statuses": [
    "queued",
    "active",
    "blocked",
    "done",
    "failed",
    "superseded"
  ],
  "gates": [
    "grill",
    "confirm"
  ],
  "artifactTypes": {
    "spec": { "category": "specs", "versioned": true },
    "system-design": { "category": "system-designs", "versioned": true },
    "architecture": { "category": "architecture", "versioned": true },
    "record": { "versioned": false },
    "vision": { "versioned": false },
    "validation": { "versioned": false }
  }
}
